/// <reference types="vite/client" />

/**
 * Vite client type declarations.
 *
 * Provides types for:
 * - import.meta.env (Vite environment variables)
 * - Static asset imports (.svg, .png, .css modules, etc.)
 */
